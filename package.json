{
  "name": "ev3-js-wait",
  "description": "wait block for use with running the motor forever",
  "repository": "git://github.com/micro-js/wait.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
